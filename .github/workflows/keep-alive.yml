name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sf https://dmarc-pipeline.onrender.com/health && echo " OK" || echo " FAIL"
